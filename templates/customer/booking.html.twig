{% extends "customer/base_customer.html.twig" %}

{% block body %}

    <div class="d-flex justify-content-center bg-light">
        <div class=" container py-5">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <h3 class="text-center">Reservation</h3>
                            <div class="card-body">

                                <div class="col-auto ">
                                    {{ form_start(form) }}
                                    {{ form_widget(form)}}
                                        <button class="btn btn-primary mt-3" type="submit">Validez ma réservation</button>
                                    {{ form_end(form)}}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div

{% endblock %}